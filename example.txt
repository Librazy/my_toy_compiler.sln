using std::conio
using std::string

[#entry(i32,string[])]
fn Example=>i:i32->k:string[]->i32
{
	i:i32?[]=k.map((s:string->i32?)=> s.substr(0,1).cast<int>() )
	j:i32[]=i.filter(!empty)
	i.map(println);j.map(println)
}
